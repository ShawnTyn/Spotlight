import { ArticleLayout } from '@/components/ArticleLayout'

export const article = {
    author: '季顺天',
    date: '2024-09-12',
    title: '我的2024届毕业设计',
    description: '可拓展的用户友好型大模型应用与本地大模型微调',
}

export const metadata = {
    title: article.title,
    description: article.description,
}

export default (props) => <ArticleLayout article={article} {...props} />

可拓展的用户友好型AIGC应用与本地大模型微调

## 简介

基于本地私有化部署的开源LLM和OpenAI API等的上层应用。以及本地大模型微调、部署、接入。

## 环境要求

1. 系统推荐 Ubuntu >= 22.04
2. GPU 推荐 Nvidia a系列（30xx）及以上，显存 >= 24G（最低6G）
3. cuda >= 12
4. nodejs >= 18
5. miniconda

## 项目架构

晚点再写······